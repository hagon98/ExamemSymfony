{% extends "base.html.twig" %} 
 {% block body %}

{{ form_start(formulaire) }}
{{ form_errors(formulaire)}}
{{ form_widget(formulaire.contenu, {'attr': {'class': 'form-control mt-3',
                                             
                                                                                      
                                                }}) }}

<button type="submit" class="btn btn-primary">Modifier</button>

{{ form_end(formulaire) }}
{% endblock %}